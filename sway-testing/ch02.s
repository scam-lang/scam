    var x = 1000000;
    var f = 1.0 / 16;
    function smaller(amount,fraction)
        {
        inspect(amount * fraction);
        }



/****************************************************/

function test()
    {
    println("Chapter 2 test...\n");
    print("smaller(x,f)...\n    ");
    smaller(x,f);
    }

test();
