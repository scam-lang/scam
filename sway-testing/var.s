include("sway.lib");

var x;
var y = 2;
var a,b,c;
var u = 1,v,w = 3;

function square(x)
    {
    function z(a,b,c) { var x = 3,y = 2; 1; }
    q = 5;
    if (m ==== z)
        {
        a;
        }
    else if (x > y)
        {
        var b = 3;
        }
    f(q,z,r);
    x * x;
    }

function bundle(x,y) { this; }

pretty(square);
ppTable(square);
inspect(this);
inspect(square);
inspect(+);
inspect(bundle(1,2));
inspect(catch(throw('ouch,5)));
