(define (sqr x)
    (return (* x x))
    )

(define (a+b a b)
    (return (sqr (+ a b)))
    )

(define z  10)

(print "zeta of "  z  " and "  z  " is "  (a+b z z) "\n")
------------------------------------------
SYNTAX ERROR: :syntaxError
a.s,line 1: an expression was expected, found CLOSE_PARENTHESIS instead
error occurred prior to: [END OF LINE]
