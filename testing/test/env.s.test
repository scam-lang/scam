(define spot ScamEnv)

(while (!= spot nil)
    (define s (car spot))
    (println (prefix s (stringUntil s "=")))
    (set! spot (cdr spot))
    )
------------------------------------------
SHELL
TERM
XDG_SESSION_COOKIE
SSH_CLIENT
SSH_TTY
USER
LS_COLORS
PATH
MAIL
PWD
EDITOR
LANG
HISTCONTROL
SPEECHD_PORT
HOME
SHLVL
LOGNAME
SSH_CONNECTION
LESSOPEN
EMAIL
DISPLAY
LESSCLOSE
_
