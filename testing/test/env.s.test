var spot = SwayEnv;

while (spot != :null)
    {
    var s = head(spot);
    println(prefix(s,stringUntil(s,"=")));
    spot = tail(spot);
    }

------------------------------------------
ORBIT_SOCKETDIR
SSH_AGENT_PID
SHELL
TERM
XDG_SESSION_COOKIE
GTK_RC_FILES
WINDOWID
GTK_MODULES
USER
LS_COLORS
GNOME_KEYRING_SOCKET
SSH_AUTH_SOCK
USERNAME
SESSION_MANAGER
PATH
DESKTOP_SESSION
PWD
GDM_KEYBOARD_LAYOUT
EDITOR
GNOME_KEYRING_PID
LANG
GDM_LANG
GDMSESSION
HISTCONTROL
SPEECHD_PORT
HOME
SHLVL
GNOME_DESKTOP_SESSION_ID
LOGNAME
XDG_DATA_DIRS
DBUS_SESSION_BUS_ADDRESS
LESSOPEN
EMAIL
DISPLAY
LESSCLOSE
XAUTHORITY
COLORTERM
_
