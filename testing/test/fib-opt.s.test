(include "compile.lib")
(include "pretty.lib")

(define (fib n)
    (if (< n 2)
        n
        (+ (fib (- n 1)) (fib (- n 2)))
        )
    )

(define x 25)
(define result)

(define s (time))
(define result (fib x))
(define f (time))
(println "(fib " x ") is " result ", " (- f s) " seconds.")

(println "compiling fib...")
(compile fib this)
(println "fib compiled.")

(define s (time))
(define result (fib x))
(define f (time))
(println "(fib " x ") is " result ", " (- f s) " seconds.")
------------------------------------------
gc:1, 11275 cells
gc:2, 11264 cells
gc:3, 11246 cells
gc:4, 11275 cells
gc:5, 11244 cells
gc:6, 11228 cells
(fib 25) is 75025, 23 seconds.
compiling fib...
fib compiled.
gc:7, 11359 cells
gc:8, 11333 cells
gc:9, 11328 cells
gc:10, 11354 cells
gc:11, 11333 cells
gc:12, 11328 cells
gc:13, 11317 cells
(fib 25) is 75025, 9 seconds.
