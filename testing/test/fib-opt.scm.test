(include "compile.lib")

(define (fib n)
    (if (< n 2)
        n
        (+ (fib (- n 1)) (fib (- n 2)))
        )
    )

(define x 23)
(define result)

(define s (time))
(define result (fib x))
(define f (time))
(println "(fib " x ") is " result ", " (- f s) " seconds.")

(println "compiling fib...")
(compile fib this)
(println "fib compiled.")

(define s (time))
(define result (fib x))
(define f (time))
(println "(fib " x ") is " result ", " (- f s) " seconds.")
------------------------------------------
gc 1: 0.004918s (total 0.004918s), 1986898 cells free
gc 2: 0.005068s (total 0.009986s), 1986894 cells free
gc 3: 0.005114s (total 0.015100s), 1986977 cells free
(fib 23) is 28657, 1.3038280010 seconds.
compiling fib...
fib compiled.
gc 4: 0.004769s (total 0.019869s), 1986859 cells free
gc 5: 0.005122s (total 0.024991s), 1986859 cells free
gc 6: 0.005114s (total 0.030105s), 1986859 cells free
(fib 23) is 28657, 0.7821230888 seconds.
