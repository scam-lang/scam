(include "pretty.lib")

(define if
    (lambda (# test $tBranch $)
        (define oldIf (get 'if (get '__context __context)))
        (pp oldIf)
        (println "the test is "  test)
        (pass oldIf # test $tBranch $)
        )
    )

(pp if)

(println "testing if 3 is less than 4...")
(if (< 3 4)
    (println "three *is* less than 4!")
    (println "oops!")
    )
------------------------------------------
<object 14724>
                       __label  : closure
                     __context  : <environment 13423>
                          name  : anonymous
                    parameters  : (# test $tBranch $)
                          code  : (begin (define oldIf (get (quote if) (get (quote __context) __context))) (pp oldIf) (println "the test is " test) (pass oldIf # test $tBranch $))
testing if 3 is less than 4...
<object 1831>
                       __label  : builtIn
                     __context  : <environment 73>
                          name  : if
                    parameters  : (# test $then $)
                          code  : 90
the test is #t
three *is* less than 4!
